# =====================================================================
# Ice Stream — Production Backend Dependencies
# =====================================================================

# FastAPI Web Server & REST / WebSocket Engine
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Data Processing & Storage (Lakehouse & Catalog)
pandas>=2.0.0
pyarrow>=13.0.0
duckdb>=0.8.0
boto3>=1.34.0

# Streaming & Messaging (Kafka)
confluent-kafka>=2.3.0
kafka-python>=2.0.2

# Email / Contact Us
# (Uses Python built-in smtplib & email modules)